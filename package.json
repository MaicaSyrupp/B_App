{
  "name": "b-app-lms",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "electron:dev": "npm run electron:dev --workspace=web",
    "electron:build": "npm run electron:build --workspace=web",
    "build": "npm run build --workspace=web"
  }
}
